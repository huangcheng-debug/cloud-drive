services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      # SQLite 数据库持久化
      - db_data:/app/prisma
      # 上传文件持久化
      - uploads_data:/app/uploads
    environment:
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  db_data:
  uploads_data: